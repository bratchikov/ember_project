import addPause from 'first-pro/utils/add-pause';
import { module, test } from 'qunit';

module('Unit | Utility | add pause');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addPause();
  assert.ok(result);
});
