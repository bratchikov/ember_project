import moreProgram from 'first-pro/utils/more-program';
import { module, test } from 'qunit';

module('Unit | Utility | more program');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = moreProgram();
  assert.ok(result);
});
