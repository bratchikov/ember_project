
import { currentAds } from 'first-pro/helpers/current-ads';
import { module, test } from 'qunit';

module('Unit | Helper | current ads');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currentAds([42]);
  assert.ok(result);
});

