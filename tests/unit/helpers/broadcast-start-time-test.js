
import { broadcastStartTime } from 'first-pro/helpers/broadcast-start-time';
import { module, test } from 'qunit';

module('Unit | Helper | broadcast start time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = broadcastStartTime([42]);
  assert.ok(result);
});

