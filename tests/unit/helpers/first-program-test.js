
import { firstProgram } from 'first-pro/helpers/first-program';
import { module, test } from 'qunit';

module('Unit | Helper | first program');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = firstProgram([42]);
  assert.ok(result);
});

