
import { currentMoment } from 'first-pro/helpers/current-moment';
import { module, test } from 'qunit';

module('Unit | Helper | current moment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currentMoment([42]);
  assert.ok(result);
});

