
import { hardClass } from 'first-pro/helpers/hard-class';
import { module, test } from 'qunit';

module('Unit | Helper | hard class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hardClass([42]);
  assert.ok(result);
});

